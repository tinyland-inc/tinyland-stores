/**
 * Railway-oriented error handling for color operations.
 *
 * Extracted from src/lib/types/color.ts to avoid $lib/ dependency.
 * Consumers may also import these from @tummycrypt/tinyland-types.
 */
export {};
